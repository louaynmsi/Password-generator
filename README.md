# Password-generator
Password Generator &amp; Strength Analyzer — a Python CLI tool that generates secure passwords based on user preferences (length, character types, exclusions), then instantly evaluates each password's strength, estimates how long it would take to brute-force, and shows a character breakdown. Passwords can be copied to clipboard or saved to a file.
